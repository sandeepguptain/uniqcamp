# Contact API (server)
PORT=3007
CONTACT_TO_EMAIL=in.bebigupta@gmail.com

# Optional: send demo requests by email via SMTP (server loads .env from project root)
# For Gmail: enable 2FA, then create an App Password at https://myaccount.google.com/apppasswords and use it as SMTP_PASS
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=your@gmail.com

# Frontend: point form to your API (dev uses /api/contact via proxy if unset)
# VITE_CONTACT_FORM_ENDPOINT=/api/contact
